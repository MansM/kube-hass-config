# homeassistant:
#   # Name of the location where Home Assistant is running
#   name: Home
#   unit_system: metric

hue:
  bridges:
    - host: 10.0.2.213
      allow_unreachable: true
      allow_hue_groups: true

# http:
# frontend:
